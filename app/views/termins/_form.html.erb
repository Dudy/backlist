<%= form_for(@termin) do |f| %>
  <% if @termin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@termin.errors.count, "error") %> prohibited this termin from being saved:</h2>

      <ul>
      <% @termin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datum %><br />
    <%= f.datetime_select :datum %>
  </div>
  <div class="field">
    <%= f.label :beschreibung %><br />
    <%= f.text_field :beschreibung %>
  </div>
  <div class="field">
    <%= f.label :benutzer_id %><br />
    <%= f.text_field :benutzer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
